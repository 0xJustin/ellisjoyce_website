# Storyblok read token (preview token for local dev, public token for production builds)
STORYBLOK_DELIVERY_API_TOKEN=

# Hosted Streamlit app for /tools/big-day.
# PUBLIC_BIG_DAY_OPTIMIZER_URL=https://big-day.ellis-joyce.com

# Optional: override content version. Defaults to draft in dev and published in build.
# STORYBLOK_CONTENT_VERSION=draft

# Optional: set only if your space is outside EU.
# STORYBLOK_REGION=us
